# Skills Directory

Use this folder for project-specific or company-specific reusable frontend skills layered on top of the base React guidance.

Examples:

- Internal design system usage rules
- Product-specific analytics conventions
- Internationalization rules
- Internal API contract patterns
- Company-specific accessibility standards

Keep the shared frontend engineering rules in `context/`, `rules/`, `prompts/`, and `checklists/`.
