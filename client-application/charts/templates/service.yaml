apiVersion: v1
kind: Service
metadata:
  name: {{ include "dc-client.fullname" . }}
  labels:
    {{- include "dc-client.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dc-client.selectorLabels" . | nindent 4 }}
