# Skills Directory

Use this folder for project-specific or domain-specific reusable skills layered on top of the base Spring Boot guidance.

Examples:

- Payments integration rules
- Multi-tenant data access rules
- Event-driven messaging conventions
- Company-specific audit requirements
- Internal API standards

Keep the shared architectural and engineering rules in `context/`, `rules/`, and `prompts/`.
