apiVersion: v1
kind: Service
metadata:
  name: {{ include "dc-backend.fullname" . }}
  labels:
    {{- include "dc-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dc-backend.selectorLabels" . | nindent 4 }}
