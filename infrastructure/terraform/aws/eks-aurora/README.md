# AWS VPC + EKS + Aurora PostgreSQL

Creates:
- VPC + private worker subnets
- EKS cluster (managed node group)
- Aurora PostgreSQL in private subnets

Use this when deploying backend/frontend with Helm to EKS.
