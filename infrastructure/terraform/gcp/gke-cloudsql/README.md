# GCP VPC + GKE + Cloud SQL PostgreSQL

Creates:
- VPC + subnet + secondary ranges
- GKE cluster + node pool
- Cloud SQL for PostgreSQL (private IP)

Use this when deploying backend/frontend with Helm to GKE.
