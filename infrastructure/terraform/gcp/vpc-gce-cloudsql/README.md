# GCP VPC + GCE + Cloud SQL PostgreSQL

Creates:
- Custom VPC + subnet
- GCE VM for backend container
- Cloud SQL for PostgreSQL (private IP)

Cloud SQL PostgreSQL is the GCP equivalent used here for Aurora PostgreSQL learning parity.
